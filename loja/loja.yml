spring:
  datasource:
    password: root
    username: root
    url: 'jdbc:mysql://localhost:3306/loja'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
